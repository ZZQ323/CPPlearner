#include<bits/stdc++.h>

using namespace std;

constexpr int N=150;
int a[N],b[N];

/**
 * 第一次采用对称式的写法，但是a[i]第一次交换之后，
 * b和a都改变了，所以maxn和minn不准确，很有可能是重复交换同一个东西
 * 
*/

int main(int argc, char const *argv[])
{
    int t;
    scanf("%d",&t);
    while(t--){
        int n,x,y;
        
    }   

    return 0;
}
